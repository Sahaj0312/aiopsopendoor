"""applyops — application as production AI Ops.

A stacked, reviewable agent pipeline that converts a job description and a
candidate's grounded facts into an honest, evaluated, observable application.
"""

__version__ = "0.0.1"
